age = 34 # integer
name = "Atharv" # string
cgpa = 4.55 # float

#assignment operator (=).
# dynamicallly typed language - python -> no need to specify datatype explicitly 
# like c,c++,Java langugae

#RUles for defining variables in python
#must start with a letter (a-z,A-Z) or an underscore(_).
#They can contain letters,numbers and underscores.
#Variable names are case- sensitive (age and Age are different).
#Avoid using python keywords (e.g.,if,for,while) as variable names.

# 34age = 4#Inavlid beacuse variable cannot start with a number.
age = 32#valid because variable can start with a number.
# a$$ge = 45 #Invalid because varibales cannot contain special characters other than (_).
_age = 45 #Valid because starts with a underscore
__nice_ = 32#Valid because it starts with underscore
a_b_c_7 = "Sam"#Valid